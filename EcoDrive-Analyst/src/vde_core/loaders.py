import pandas as pd

def load_cycle_csv(path_or_file) -> pd.DataFrame:
    """
    Load a drive cycle CSV with columns t, v.
    Placeholder: return empty DataFrame.
    """
    return pd.DataFrame()
